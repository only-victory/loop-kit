<!doctype html>
<!-- loop-kit / slide : 디자인 결 [소프트 글래스]
     정체성 : 인디고 그라데이션 배경, 글래스(블러) 카드, 큰 라운드, 부드러운 그림자, 파스텔 강조.
              모던 SaaS 무드. 색은 결에 포함. measure로 좌측쏠림 방지. -->
<html lang="ko">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>슬라이드 제목</title>
<link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600&display=swap" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css" rel="stylesheet">
<style>
:root{
  /* 소프트 팔레트 : 인디고 베이스 + 파스텔 강조 */
  --bg-base:#0e0b1d; --bg-grad-a:#171034; --bg-grad-b:#0b1226;
  --glass:rgba(255,255,255,.055); --glass-hi:rgba(255,255,255,.11);
  --fg:#f4f2ff; --mute:#bcb8d6; --dim:#7d79a0;
  --lav:#b6a6ff; --mint:#86e6c8; --peach:#ffb59e; --sky:#8cc6ff;
  --mono:'JetBrains Mono',ui-monospace,monospace;
  --sans:'Pretendard',-apple-system,sans-serif;
  --gutter-x:118px; --anchor-y:24vh; --measure:58ch; --r:24px;
}
*{box-sizing:border-box;margin:0;padding:0;}
html,body{height:100%;}
body{font-family:var(--sans);color:var(--mute);overflow:hidden;word-break:keep-all;
  background:radial-gradient(1200px 700px at 78% 18%,rgba(182,166,255,.16),transparent 60%),
             radial-gradient(900px 600px at 12% 88%,rgba(134,230,200,.12),transparent 55%),
             linear-gradient(155deg,var(--bg-grad-a),var(--bg-grad-b));}

.slide{position:absolute;inset:0;display:none;}
.slide.active{display:block;animation:fade .3s cubic-bezier(.2,.8,.2,1);}
@keyframes fade{from{opacity:0;transform:translateY(12px);}to{opacity:1;transform:none;}}
@media(prefers-reduced-motion:reduce){.slide.active{animation:none;}}

.frame{position:absolute;left:var(--gutter-x);right:var(--gutter-x);top:var(--anchor-y);}
.eyebrow{font-family:var(--mono);font-size:14px;letter-spacing:.18em;text-transform:uppercase;color:var(--lav);font-weight:600;margin:0 0 20px;display:inline-block;padding:7px 16px;border-radius:999px;background:var(--glass);border:1px solid var(--glass-hi);}
.title{font-size:60px;font-weight:800;color:var(--fg);margin:0 0 22px;line-height:1.12;letter-spacing:-.02em;}
.title.hero{font-size:clamp(58px,7.6vw,108px);line-height:1.05;}
.title .grad{background:linear-gradient(90deg,var(--lav),var(--mint));-webkit-background-clip:text;background-clip:text;color:transparent;}
.lead{font-size:22px;line-height:1.62;color:var(--mute);max-width:var(--measure);}
.body{margin-top:24px;max-width:var(--measure);}
.body ul{list-style:none;padding:0;display:flex;flex-direction:column;gap:14px;}
.body li{font-size:21px;line-height:1.6;color:var(--mute);padding-left:28px;position:relative;}
.body li::before{content:'\2022';position:absolute;left:4px;color:var(--lav);font-size:22px;line-height:1.4;}

.mark{position:fixed;top:28px;left:30px;color:var(--lav);font-size:16px;z-index:10;text-shadow:0 0 16px rgba(182,166,255,.7);}
.counter{position:fixed;bottom:28px;right:32px;color:var(--dim);font-size:15px;z-index:10;font-family:var(--mono);}
.folio{position:fixed;top:28px;right:32px;font-family:var(--mono);font-size:14px;letter-spacing:.12em;color:var(--dim);z-index:10;}
:focus-visible{outline:2px solid var(--lav);outline-offset:3px;}

/* 공통 글래스 카드 베이스 */
.col,.stat,.step,.pp,.code,.cmp,.outro .cta .row{
  background:var(--glass);border:1px solid var(--glass-hi);backdrop-filter:blur(14px);-webkit-backdrop-filter:blur(14px);}

/* two-column */
.two-column{display:grid;grid-template-columns:1fr 1fr;gap:20px;margin-top:24px;}
.col{padding:30px 32px;border-radius:var(--r);box-shadow:0 16px 40px rgba(0,0,0,.28);}
.col .lbl{font-family:var(--mono);font-size:14px;letter-spacing:.12em;font-weight:600;color:var(--lav);margin-bottom:14px;text-transform:uppercase;}
.col.alt .lbl{color:var(--mint);}
.col .body{font-size:23px;color:var(--fg);line-height:1.45;margin-top:0;font-weight:600;}
.col p{font-size:19px;color:var(--mute);line-height:1.55;margin-top:10px;}

/* stats-split */
.stats{display:grid;grid-template-columns:1fr 1fr;gap:20px;margin-top:24px;}
.stat{padding:42px 30px;border-radius:var(--r);text-align:center;box-shadow:0 16px 40px rgba(0,0,0,.28);}
.stat .v{font-size:104px;font-weight:800;line-height:1;letter-spacing:-.04em;background:linear-gradient(90deg,var(--lav),var(--sky));-webkit-background-clip:text;background-clip:text;color:transparent;}
.stat.alt .v{background:linear-gradient(90deg,var(--mint),var(--lav));-webkit-background-clip:text;background-clip:text;color:transparent;}
.stat .l{margin-top:16px;font-size:19px;color:var(--mute);}

/* comparison-table */
.cmp{width:100%;border-collapse:separate;border-spacing:0;margin-top:24px;font-size:19px;border-radius:var(--r);overflow:hidden;}
.cmp th,.cmp td{text-align:left;padding:17px 20px;border-bottom:1px solid var(--glass-hi);}
.cmp thead th{font-family:var(--mono);font-size:14px;letter-spacing:.1em;text-transform:uppercase;color:var(--lav);font-weight:600;}
.cmp tbody th{color:var(--fg);font-weight:700;}
.cmp td{color:var(--mute);}
.cmp tr:last-child th,.cmp tr:last-child td{border-bottom:none;}
.cmp .yes{color:var(--mint);} .cmp .no{color:var(--peach);}

/* steps-list */
.steps{display:flex;flex-direction:column;gap:14px;margin-top:24px;}
.step{display:flex;align-items:flex-start;gap:22px;padding:20px 28px;border-radius:18px;box-shadow:0 10px 26px rgba(0,0,0,.22);}
.step .n{font-family:var(--mono);font-size:16px;font-weight:600;color:var(--lav);min-width:42px;padding-top:2px;}
.step .nm{font-size:22px;font-weight:700;color:var(--fg);min-width:170px;flex-shrink:0;}
.step .d{font-size:19px;color:var(--mute);flex:1;line-height:1.5;}

/* principle */
.principle{display:grid;grid-template-columns:1fr 1fr;gap:20px;margin-top:24px;}
.pp{padding:26px 30px;border-radius:var(--r);font-size:22px;line-height:1.45;box-shadow:0 12px 30px rgba(0,0,0,.24);}
.pp .mk{font-family:var(--mono);font-size:14px;font-weight:600;display:block;margin-bottom:12px;letter-spacing:.1em;text-transform:uppercase;}
.pp.good{border-color:rgba(134,230,200,.5);} .pp.good .mk{color:var(--mint);} .pp.good{color:var(--fg);}
.pp.bad{border-color:rgba(255,181,158,.5);} .pp.bad .mk{color:var(--peach);} .pp.bad{color:var(--mute);}

/* quote */
.quote{margin-top:24px;max-width:22ch;}
.quote blockquote{font-size:clamp(40px,5vw,70px);font-weight:800;line-height:1.16;letter-spacing:-.02em;background:linear-gradient(90deg,var(--lav),var(--mint));-webkit-background-clip:text;background-clip:text;color:transparent;}
.quote .by{margin-top:28px;font-family:var(--mono);font-size:16px;letter-spacing:.1em;color:var(--lav);text-transform:uppercase;max-width:none;}

/* code / code-split */
.code{margin-top:24px;border-radius:18px;overflow:hidden;box-shadow:0 16px 40px rgba(0,0,0,.3);}
.code .bar{font-family:var(--mono);font-size:14px;color:var(--dim);padding:12px 20px;border-bottom:1px solid var(--glass-hi);letter-spacing:.08em;}
.code pre{font-family:var(--mono);font-size:18px;line-height:1.7;color:var(--fg);padding:22px 24px;background:rgba(0,0,0,.22);overflow:auto;}
.code-split{display:grid;grid-template-columns:1fr 1fr;gap:20px;margin-top:24px;}
.code-split .code{margin-top:0;}
.code .tok{color:var(--lav);} .code .str{color:var(--mint);} .code .dim{color:var(--dim);}

/* module-intro */
.module-intro{position:absolute;inset:0;display:flex;flex-direction:column;justify-content:center;align-items:center;text-align:center;padding:0 var(--gutter-x);gap:24px;}
.module-intro .num{font-family:var(--mono);font-size:15px;letter-spacing:.22em;color:var(--lav);text-transform:uppercase;padding:9px 22px;border-radius:999px;background:var(--glass);border:1px solid var(--glass-hi);}
.module-intro .ttl{font-size:clamp(62px,8.5vw,116px);font-weight:800;line-height:1.02;letter-spacing:-.03em;background:linear-gradient(90deg,var(--lav),var(--mint));-webkit-background-clip:text;background-clip:text;color:transparent;}
.module-intro .sub{font-size:22px;color:var(--mute);max-width:52ch;line-height:1.5;}

/* outro */
.outro{position:absolute;inset:0;display:flex;flex-direction:column;justify-content:center;padding:0 var(--gutter-x);}
.outro .big{font-size:clamp(46px,6vw,90px);font-weight:800;line-height:1.06;color:var(--fg);max-width:18ch;letter-spacing:-.02em;}
.outro .cta{margin-top:34px;display:flex;flex-direction:column;gap:14px;max-width:60ch;}
.outro .cta .row{display:flex;align-items:baseline;gap:18px;font-size:21px;color:var(--fg);padding:18px 26px;border-radius:16px;box-shadow:0 10px 26px rgba(0,0,0,.2);}
.outro .cta .row .k{font-family:var(--mono);font-size:14px;color:var(--mint);letter-spacing:.1em;text-transform:uppercase;min-width:110px;flex-shrink:0;}
</style>
</head>
<body>
  <div class="mark">✦</div>
  <div class="folio">SOFT GLASS</div>

  <!-- HERO -->
  <section class="slide active">
    <div class="frame">
      <div class="eyebrow">Section 01</div>
      <h1 class="title hero">핵심은 <span class="grad">한 문장</span>으로</h1>
      <p class="lead">부제는 한 줄로. 청중이 이 장에서 무엇을 가져가는지 여기서 정해진다.</p>
    </div>
  </section>

  <!-- CONTENT -->
  <section class="slide">
    <div class="frame">
      <div class="eyebrow">맥락</div>
      <h1 class="title">왜 지금 이 이야기인가</h1>
      <div class="body">
        <ul>
          <li>주장을 떠받치는 첫 번째 근거. 한 줄로 끊어 읽히게 쓴다.</li>
          <li>두 번째 근거. 문단을 통째로 넣지 않는다.</li>
          <li>세 번째 근거. 셋을 넘기면 청중이 잊는다.</li>
        </ul>
      </div>
    </div>
  </section>

  <!-- TWO-COLUMN -->
  <section class="slide">
    <div class="frame">
      <div class="eyebrow">대비</div>
      <h1 class="title">두 갈래의 선택</h1>
      <div class="two-column">
        <div class="col"><div class="lbl">기존 방식</div><div class="body">설명을 여기에</div><p>부연 한 줄.</p></div>
        <div class="col alt"><div class="lbl">개선 방식</div><div class="body">설명을 여기에</div><p>부연 한 줄.</p></div>
      </div>
    </div>
  </section>

  <!-- STATS-SPLIT -->
  <section class="slide">
    <div class="frame">
      <div class="eyebrow">수치</div>
      <h1 class="title">숫자가 말한다</h1>
      <div class="stats">
        <div class="stat"><div class="v">312</div><div class="l">처리 건수 (월)</div></div>
        <div class="stat alt"><div class="v">78%</div><div class="l">자동 응답 비율</div></div>
      </div>
    </div>
  </section>

  <!-- COMPARISON-TABLE -->
  <section class="slide">
    <div class="frame">
      <div class="eyebrow">비교표</div>
      <h1 class="title">플랜 비교</h1>
      <table class="cmp">
        <thead><tr><th>항목</th><th>A 플랜</th><th>B 플랜</th><th>C 플랜</th></tr></thead>
        <tbody>
          <tr><th>가격</th><td>무료</td><td>9,900원</td><td>29,000원</td></tr>
          <tr><th>사용자</th><td>1명</td><td>5명</td><td>무제한</td></tr>
          <tr><th>지원</th><td class="no">없음</td><td class="yes">이메일</td><td class="yes">전담</td></tr>
        </tbody>
      </table>
    </div>
  </section>

  <!-- STEPS-LIST -->
  <section class="slide">
    <div class="frame">
      <div class="eyebrow">절차</div>
      <h1 class="title">세 단계로 끝난다</h1>
      <div class="steps">
        <div class="step"><span class="n">01</span><span class="nm">단계명</span><span class="d">설명을 여기에 한 줄로.</span></div>
        <div class="step"><span class="n">02</span><span class="nm">단계명</span><span class="d">설명을 여기에 한 줄로.</span></div>
        <div class="step"><span class="n">03</span><span class="nm">단계명</span><span class="d">설명을 여기에 한 줄로.</span></div>
      </div>
    </div>
  </section>

  <!-- PRINCIPLE -->
  <section class="slide">
    <div class="frame">
      <div class="eyebrow">원칙</div>
      <h1 class="title">이렇게, 저렇게 말고</h1>
      <div class="principle">
        <div class="pp good"><span class="mk">이렇게</span>지켜야 할 방식을 구체적으로.</div>
        <div class="pp bad"><span class="mk">이렇게 말고</span>피해야 할 방식을 구체적으로.</div>
      </div>
    </div>
  </section>

  <!-- QUOTE -->
  <section class="slide">
    <div class="frame">
      <div class="eyebrow">인용</div>
      <div class="quote">
        <blockquote>검증 없으면, 그저 예쁜 쓰레기다.</blockquote>
        <div class="by">loop-kit 제1원칙</div>
      </div>
    </div>
  </section>

  <!-- CODE-SPLIT -->
  <section class="slide">
    <div class="frame">
      <div class="eyebrow">예제</div>
      <h1 class="title">코드와 결과</h1>
      <div class="code-split">
        <div class="code"><div class="bar">verify.sh</div><pre><span class="dim"># 게이트 실행</span>
bash verify.sh deck.html</pre></div>
        <div class="code"><div class="bar">출력</div><pre>슬라이드 수 : 11장
<span class="str">✓ HARD 위반 0건</span> : 통과</pre></div>
      </div>
    </div>
  </section>

  <!-- MODULE-INTRO -->
  <section class="slide">
    <div class="module-intro">
      <div class="num">Part 02</div>
      <h2 class="ttl">실전 적용</h2>
      <p class="sub">섹션을 여는 한 줄. 무엇을 다루는지 미리 예고한다.</p>
    </div>
  </section>

  <!-- OUTRO -->
  <section class="slide">
    <div class="outro">
      <div class="eyebrow">다음 행동</div>
      <div class="big">오늘, 한 가지만 시작하라.</div>
      <div class="cta">
        <div class="row"><span class="k">지금</span><span>템플릿 하나 복제해 첫 덱 만들기</span></div>
        <div class="row"><span class="k">이번 주</span><span>verify 게이트를 워크플로에 붙이기</span></div>
      </div>
    </div>
  </section>

  <div class="counter"><span id="cur">1</span> / <span id="tot">1</span></div>
<script>
(function(){
  var slides=Array.prototype.slice.call(document.querySelectorAll('.slide'));
  var i=0,buf='';
  document.getElementById('tot').textContent=slides.length;
  function show(n){
    i=Math.max(0,Math.min(slides.length-1,n));
    slides.forEach(function(s,idx){s.classList.toggle('active',idx===i);});
    document.getElementById('cur').textContent=i+1;
  }
  document.addEventListener('keydown',function(e){
    var t=e.target.tagName;
    if(t==='INPUT'||t==='TEXTAREA')return;
    switch(e.key){
      case 'ArrowRight':case ' ':case 'PageDown':case 'j':case 'l':show(i+1);break;
      case 'ArrowLeft':case 'PageUp':case 'k':case 'h':show(i-1);break;
      case 'Home':show(0);break;
      case 'End':show(slides.length-1);break;
      case 'f':document.fullscreenElement?document.exitFullscreen():document.documentElement.requestFullscreen();break;
      case 'Escape':if(document.fullscreenElement)document.exitFullscreen();break;
      case 'Enter':if(buf){show(parseInt(buf,10)-1);buf='';}break;
      default:if(/^[0-9]$/.test(e.key)){buf+=e.key;return;}
    }
    buf='';
  });
  var sx=0,sy=0;
  document.addEventListener('touchstart',function(e){sx=e.changedTouches[0].clientX;sy=e.changedTouches[0].clientY;},{passive:true});
  document.addEventListener('touchend',function(e){
    var dx=e.changedTouches[0].clientX-sx,dy=e.changedTouches[0].clientY-sy;
    if(Math.abs(dx)>80&&Math.abs(dy)<40){dx<0?show(i+1):show(i-1);}
  },{passive:true});
  show(0);
})();
</script>
</body>
</html>
