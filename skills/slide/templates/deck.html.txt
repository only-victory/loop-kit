<!doctype html>
<!-- loop-kit / slide 완성형 베이스. 이 파일을 복제해 각 섹션(.slide) 안을 채운다.
     확장자는 .txt (생성물 .html과 구분). 실제 산출 시 .html로 저장한다.
     ★ 모든 슬라이드는 .frame 안에 .eyebrow / .title / .lead / .body 순서로 채운다.
       좌측 거터(left)와 제목 높이(top)가 고정되어 장마다 위치가 흔들리지 않는다.
       슬라이드별로 padding·위치를 새로 잡지 말 것. -->
<html lang="ko">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>슬라이드 제목</title>
<style>
  :root{
    --bg-base:#0a0a0f; --bg-elev-1:#13131a; --bg-elev-2:#1c1c26;
    --text-hi:#f5f5fa; --text-md:#cfcfdc; --text-lo:#8a8aa0;
    --border-subtle:#22222e; --border-base:#2e2e3d;
    --accent-blue:#5b8cff; --accent-purple:#a371ff;
    --accent-grad:linear-gradient(90deg,#5b8cff,#a371ff);
    --success:#4ade80; --warning:#fbbf24; --danger:#ff6b7a; --info:#60a5fa;
    /* 레이아웃 고정값 : 모든 슬라이드가 공유한다 */
    --gutter-x:128px;   /* 좌우 거터 */
    --anchor-y:26vh;    /* 제목(eyebrow) 시작 높이 — 장마다 동일 */
  }
  *{box-sizing:border-box;}
  html,body{margin:0;height:100%;}
  body{
    background:var(--bg-base); color:var(--text-md);
    font-family:system-ui,-apple-system,"Segoe UI",Roboto,sans-serif;
    overflow:hidden;
  }
  .slide{position:absolute; inset:0; display:none;}
  .slide.active{display:block; animation:fade .24s cubic-bezier(.2,.8,.2,1);}
  @keyframes fade{from{opacity:0;transform:translateY(8px);}to{opacity:1;transform:none;}}

  /* ★ 고정 프레임 : 모든 슬라이드가 같은 좌측·높이에서 시작 */
  .frame{
    position:absolute;
    left:var(--gutter-x); right:var(--gutter-x);
    top:var(--anchor-y);
  }
  .eyebrow{
    font-size:14px; letter-spacing:.16em; text-transform:uppercase;
    color:var(--text-lo); font-weight:600; margin:0 0 16px;
  }
  .title{font-size:60px; font-weight:700; color:var(--text-hi);
         margin:0 0 24px; line-height:1.14;}
  .title.hero{font-size:clamp(58px,7.5vw,108px); font-weight:800; line-height:1.08;}
  .lead{font-size:20px; line-height:1.6; color:var(--text-md); margin:0; max-width:60ch;}
  .body{margin-top:24px;}
  .body ul{margin:0; padding-left:1.1em;}
  .body li{font-size:20px; line-height:1.7; margin:0 0 12px;}
  .grad{background:var(--accent-grad); -webkit-background-clip:text;
        background-clip:text; color:transparent;}

  /* 2단 (two-column) : 같은 프레임 안에서 균등 분할 */
  .cols{display:grid; grid-template-columns:1fr 1fr; gap:32px; margin-top:24px;}
  .card{background:var(--bg-elev-1); border:1px solid var(--border-base);
        border-radius:16px; padding:32px;}
  .card h3{font-size:36px; margin:0 0 8px; color:var(--text-hi);}

  /* 모티프 */
  .mark{position:fixed; top:24px; left:24px; color:var(--text-lo); font-size:16px; z-index:10;}
  .counter{position:fixed; bottom:24px; right:24px; color:var(--text-lo); font-size:16px; z-index:10;}
  :focus-visible{outline:2px solid var(--accent-blue); outline-offset:3px;}
  @media (prefers-reduced-motion:reduce){ .slide.active{animation:none;} }
</style>
</head>
<body>
  <div class="mark">✦</div>

  <!-- ===== 한 장 = 메시지 한 개. 항상 .frame > .eyebrow/.title/.lead/.body ===== -->
  <section class="slide active">
    <div class="frame">
      <div class="eyebrow">SECTION 01</div>
      <h1 class="title hero">핵심 메시지를 <span class="grad">한 문장</span>으로</h1>
      <p class="lead">부제는 한 줄로.</p>
    </div>
  </section>

  <section class="slide">
    <div class="frame">
      <div class="eyebrow">우리가 아는 규칙</div>
      <h1 class="title">200년 동안, 공식은 하나였다</h1>
      <div class="body">
        <ul>
          <li>근거 1 : 짧게</li>
          <li>근거 2 : 짧게</li>
          <li>근거 3 : 짧게</li>
        </ul>
      </div>
    </div>
  </section>

  <section class="slide">
    <div class="frame">
      <div class="eyebrow">두 개의 문</div>
      <h1 class="title">두 개의 문이 동시에 닫힌다</h1>
      <div class="cols">
        <div class="card"><h3>지식노동</h3><p class="lead">사무·분석·창작이 먼저 자동화된다</p></div>
        <div class="card"><h3>육체노동</h3><p class="lead">로봇이 현장·물류·돌봄까지 들어온다</p></div>
      </div>
    </div>
  </section>
  <!-- ===== 슬라이드 끝 ===== -->

  <div class="counter"><span id="cur">1</span> / <span id="tot">1</span></div>

<script>
(function(){
  var slides = Array.prototype.slice.call(document.querySelectorAll('.slide'));
  var i = 0, buf = '';
  document.getElementById('tot').textContent = slides.length;

  function show(n){
    i = Math.max(0, Math.min(slides.length - 1, n));
    slides.forEach(function(s, idx){ s.classList.toggle('active', idx === i); });
    document.getElementById('cur').textContent = i + 1;
  }
  function next(){ show(i + 1); }
  function prev(){ show(i - 1); }

  document.addEventListener('keydown', function(e){
    var t = e.target.tagName;
    if (t === 'INPUT' || t === 'TEXTAREA') return;
    switch(e.key){
      case 'ArrowRight': case ' ': case 'PageDown': case 'j': case 'l': next(); break;
      case 'ArrowLeft':  case 'PageUp':  case 'k': case 'h': prev(); break;
      case 'Home': show(0); break;
      case 'End':  show(slides.length - 1); break;
      case 'f': if(document.fullscreenElement) document.exitFullscreen();
                else document.documentElement.requestFullscreen(); break;
      case 'Escape': if(document.fullscreenElement) document.exitFullscreen(); break;
      case 'Enter': if(buf){ show(parseInt(buf,10) - 1); buf=''; } break;
      default:
        if(/^[0-9]$/.test(e.key)){ buf += e.key; return; }
    }
    buf = '';
  });

  var sx=0, sy=0;
  document.addEventListener('touchstart', function(e){
    sx=e.changedTouches[0].clientX; sy=e.changedTouches[0].clientY;
  }, {passive:true});
  document.addEventListener('touchend', function(e){
    var dx=e.changedTouches[0].clientX - sx, dy=e.changedTouches[0].clientY - sy;
    if(Math.abs(dx) > 80 && Math.abs(dy) < 40){ dx < 0 ? next() : prev(); }
  }, {passive:true});

  show(0);
})();
</script>
</body>
</html>
