<!doctype html>
<!-- loop-kit / slide 완성형 베이스 v2 — edu 디자인 시스템 통합
     확장자 .txt (산출물 .html과 구분). 산출 시 .html로 저장.
     ★ 고정 프레임 : .frame > .eyebrow → .title → .lead / .body 순서.
       좌측 거터(--gutter-x)·제목 높이(--anchor-y)가 장마다 동일해야 한다. -->
<html lang="ko">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>슬라이드 제목</title>
<link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css" rel="stylesheet">
<style>
:root{
  /* edu 팔레트 — 프로젝터 현장 검증 완료 */
  --bg-base:#08080c; --bg-elev:#16161e; --bg-code:#1b1b26;
  --fg:#f5f5fa; --mute:#c2c2d0; --dim:#6b6b80;
  --cyan:#1ae0ff; --green:#3df58a; --yellow:#ffd91f;
  --purple:#b18cff; --blue:#4f9dff; --red:#ff6b78; --orange:#ff9f3f;
  --border:#2c2c38; --border-hi:#464654;
  --mono:'JetBrains Mono',ui-monospace,monospace;
  --sans:'Pretendard',-apple-system,sans-serif;
  /* 레이아웃 고정값 */
  --gutter-x:128px; --anchor-y:26vh;
}
*{box-sizing:border-box;margin:0;padding:0;}
html,body{height:100%;background:var(--bg-base);}
body{font-family:var(--sans);color:var(--mute);overflow:hidden;word-break:keep-all;}

/* ── 슬라이드 전환 ── */
.slide{position:absolute;inset:0;display:none;}
.slide.active{display:block;animation:fade .24s cubic-bezier(.2,.8,.2,1);}
@keyframes fade{from{opacity:0;transform:translateY(8px);}to{opacity:1;transform:none;}}
@media(prefers-reduced-motion:reduce){.slide.active{animation:none;}}

/* ── 고정 프레임 ── */
.frame{position:absolute;left:var(--gutter-x);right:var(--gutter-x);top:var(--anchor-y);}
.eyebrow{font-size:14px;letter-spacing:.16em;text-transform:uppercase;color:var(--dim);font-family:var(--mono);font-weight:700;margin:0 0 16px;}
.title{font-size:60px;font-weight:700;color:var(--fg);margin:0 0 24px;line-height:1.14;}
.title.hero{font-size:clamp(58px,7.5vw,108px);font-weight:800;line-height:1.08;}
.lead{font-size:20px;line-height:1.6;color:var(--mute);max-width:60ch;}
.body{margin-top:24px;}
.body ul{list-style:none;padding:0;display:flex;flex-direction:column;gap:10px;}
.body li{font-size:20px;line-height:1.7;color:var(--mute);}
.body li::before{content:'· ';color:var(--cyan);font-weight:700;}

/* ── 그라데이션 텍스트 ── */
.grad{background:linear-gradient(90deg,var(--cyan),var(--purple));-webkit-background-clip:text;background-clip:text;color:transparent;}
.grad-g{background:linear-gradient(90deg,var(--green),var(--cyan));-webkit-background-clip:text;background-clip:text;color:transparent;}

/* ── 모티프 ── */
.mark{position:fixed;top:24px;left:24px;color:var(--dim);font-size:16px;z-index:10;}
.counter{position:fixed;bottom:24px;right:24px;color:var(--dim);font-size:16px;z-index:10;font-family:var(--mono);}
:focus-visible{outline:2px solid var(--cyan);outline-offset:3px;}

/* ── 카드 그리드 ── */
.cards-2{display:grid;grid-template-columns:repeat(2,1fr);gap:18px;margin-top:24px;}
.cards-3{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;margin-top:24px;}
.col-card{padding:28px 30px;border:1px solid var(--border);border-radius:14px;background:var(--bg-elev);}
.col-card.cy{border-color:rgba(26,224,255,.35);}
.col-card.gr{border-color:rgba(61,245,138,.35);}
.col-card.pu{border-color:rgba(177,140,255,.35);}
.col-card.ye{border-color:rgba(255,217,31,.35);}
.col-card.rd{border-color:rgba(255,107,120,.35);}
.col-card .lbl{font-family:var(--mono);font-size:13px;letter-spacing:.16em;font-weight:700;color:var(--cyan);margin-bottom:14px;text-transform:uppercase;}
.col-card .lbl.gr{color:var(--green);} .col-card .lbl.pu{color:var(--purple);}
.col-card .lbl.ye{color:var(--yellow);} .col-card .lbl.rd{color:var(--red);}
.col-card .body{font-size:21px;color:var(--fg);line-height:1.6;}
.col-card ul{list-style:none;padding:0;margin:8px 0 0;display:flex;flex-direction:column;gap:10px;font-size:20px;color:var(--mute);line-height:1.55;}
.col-card ul li::before{content:'· ';color:var(--cyan);font-weight:700;}
.col-card.gr ul li::before{color:var(--green);} .col-card.pu ul li::before{color:var(--purple);}

/* ── 단계 목록 ── */
.steps{display:flex;flex-direction:column;gap:14px;margin-top:24px;}
.step-row{display:flex;align-items:flex-start;gap:24px;padding:20px 28px;border:1px solid var(--border);border-radius:14px;background:var(--bg-elev);}
.step-row .n{font-family:var(--mono);font-size:16px;font-weight:700;color:var(--cyan);min-width:48px;flex-shrink:0;padding-top:2px;}
.step-row .nm{font-size:22px;font-weight:700;color:var(--fg);min-width:160px;flex-shrink:0;}
.step-row .d{font-size:20px;color:var(--mute);flex:1;line-height:1.55;}
.step-row.gr .n{color:var(--green);} .step-row.rd .n{color:var(--red);}

/* ── 플로우 다이어그램 ── */
.flow{display:flex;align-items:stretch;justify-content:center;gap:12px;margin-top:24px;flex-wrap:wrap;}
.flow-node{padding:24px 20px;border:1px solid var(--border);border-radius:14px;background:var(--bg-elev);text-align:center;min-width:160px;display:flex;flex-direction:column;justify-content:center;}
.flow-node.cy{border-color:rgba(26,224,255,.45);}
.flow-node.gr{border-color:rgba(61,245,138,.4);}
.flow-node.pu{border-color:rgba(177,140,255,.4);border-style:dashed;}
.flow-node .nm{font-family:var(--mono);font-size:20px;font-weight:700;color:var(--fg);margin-bottom:6px;}
.flow-node.cy .nm{color:var(--cyan);} .flow-node.gr .nm{color:var(--green);}
.flow-node .sub{font-size:17px;color:var(--mute);}
.flow-arrow{display:flex;align-items:center;align-self:center;font-size:26px;color:var(--dim);flex-shrink:0;}

/* ── 정의 카드 ── */
.def-card{display:flex;flex-direction:column;gap:20px;padding:44px 52px;border:1px solid var(--border);border-radius:18px;background:var(--bg-elev);margin-top:24px;}
.def-card.cy{border-color:rgba(26,224,255,.4);}
.def-card.gr{border-color:rgba(61,245,138,.35);}
.def-card.pu{border-color:rgba(177,140,255,.35);}
.def-card .icon{font-family:var(--mono);font-size:13px;letter-spacing:.22em;font-weight:700;color:var(--cyan);text-transform:uppercase;}
.def-card.gr .icon{color:var(--green);} .def-card.pu .icon{color:var(--purple);}
.def-card .ttl{font-size:56px;font-weight:800;color:var(--fg);letter-spacing:-.025em;line-height:1.05;}
.def-card .line{height:3px;width:72px;background:var(--cyan);border-radius:2px;}
.def-card.gr .line{background:var(--green);} .def-card.pu .line{background:var(--purple);}
.def-card .lead{font-size:28px;color:var(--fg);font-weight:700;line-height:1.4;}
.def-card .body{font-size:20px;color:var(--mute);line-height:1.55;}

/* ── 모듈 인트로 ── */
.module-intro{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:24px;position:absolute;inset:0;text-align:center;}
.module-intro .num{font-family:var(--mono);font-size:16px;font-weight:700;letter-spacing:.28em;color:var(--cyan);text-transform:uppercase;padding:8px 22px;border:1px solid currentColor;border-radius:999px;}
.module-intro.pu .num{color:var(--purple);} .module-intro.gr .num{color:var(--green);}
.module-intro .ttl{font-size:88px;font-weight:800;letter-spacing:-.045em;line-height:1.02;color:var(--fg);}
.module-intro .sub{font-size:24px;color:var(--mute);max-width:800px;line-height:1.5;}
.module-intro .bar{width:140px;height:4px;background:linear-gradient(90deg,var(--cyan),var(--purple));border-radius:2px;}

/* ── Good/Bad 비교 ── */
.principle-pair{display:grid;grid-template-columns:1fr 1fr;gap:20px;margin-top:24px;}
.pp{padding:24px 28px;border-radius:14px;display:flex;align-items:center;gap:16px;font-size:22px;}
.pp.good{border:1px solid rgba(61,245,138,.4);border-left:4px solid var(--green);background:rgba(61,245,138,.06);color:var(--fg);}
.pp.bad{border:1px solid rgba(255,107,120,.4);border-left:4px solid var(--red);background:rgba(255,107,120,.06);color:var(--mute);}
.pp .mk{font-family:var(--mono);font-weight:700;font-size:26px;}
.pp.good .mk{color:var(--green);} .pp.bad .mk{color:var(--red);}

/* ── 수치 분할 ── */
.stat-pair{display:grid;grid-template-columns:1fr 1fr;gap:24px;margin-top:24px;}
.stat-card{padding:44px 32px;border:1px solid var(--border);border-radius:18px;background:var(--bg-elev);text-align:center;}
.stat-card.good{border-color:rgba(61,245,138,.4);background:rgba(61,245,138,.06);}
.stat-card.bad{border-color:rgba(255,107,120,.4);background:rgba(255,107,120,.06);}
.stat-card .v{font-size:108px;font-weight:800;letter-spacing:-.04em;line-height:1;color:var(--fg);}
.stat-card.good .v{color:var(--green);} .stat-card.bad .v{color:var(--red);}
.stat-card .l{margin-top:16px;font-size:20px;color:var(--mute);font-weight:600;}

/* ── 프롬프트 박스 ── */
.prompt-box{border:1px solid var(--cyan);border-left:4px solid var(--cyan);border-radius:14px;background:rgba(26,224,255,.05);padding:28px 34px;margin-top:24px;}
.prompt-box.ye{border-color:var(--yellow);border-left-color:var(--yellow);background:rgba(255,217,31,.05);}
.prompt-box.pu{border-color:var(--purple);border-left-color:var(--purple);background:rgba(177,140,255,.05);}
.prompt-box .lbl{font-family:var(--mono);font-size:13px;font-weight:700;letter-spacing:.22em;color:var(--cyan);margin-bottom:16px;text-transform:uppercase;}
.prompt-box.ye .lbl{color:var(--yellow);} .prompt-box.pu .lbl{color:var(--purple);}
.prompt-box .body{font-family:var(--mono);font-size:19px;color:var(--fg);line-height:1.75;white-space:pre-wrap;}
</style>
</head>
<body>
  <div class="mark">✦</div>

  <!-- ===== HERO ===== -->
  <section class="slide active">
    <div class="frame">
      <div class="eyebrow">SECTION 01</div>
      <h1 class="title hero">핵심 메시지를 <span class="grad">한 문장</span>으로</h1>
      <p class="lead">부제는 한 줄로. 청중이 이 장에서 무엇을 가져가는지 여기서 정해진다.</p>
    </div>
  </section>

  <!-- ===== CARDS-2 ===== -->
  <section class="slide">
    <div class="frame">
      <div class="eyebrow">비교</div>
      <h1 class="title">두 가지 선택</h1>
      <div class="cards-2">
        <div class="col-card cy">
          <div class="lbl">선택 A</div>
          <div class="body">내용을 여기에</div>
        </div>
        <div class="col-card gr">
          <div class="lbl gr">선택 B</div>
          <div class="body">내용을 여기에</div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== STEPS ===== -->
  <section class="slide">
    <div class="frame">
      <div class="eyebrow">절차</div>
      <h1 class="title">3단계로 끝납니다</h1>
      <div class="steps">
        <div class="step-row"><span class="n">01</span><span class="nm">단계명</span><span class="d">설명을 여기에</span></div>
        <div class="step-row"><span class="n">02</span><span class="nm">단계명</span><span class="d">설명을 여기에</span></div>
        <div class="step-row gr"><span class="n">03</span><span class="nm">단계명</span><span class="d">설명을 여기에</span></div>
      </div>
    </div>
  </section>

  <!-- ===== MODULE INTRO ===== -->
  <section class="slide">
    <div class="module-intro">
      <div class="num">SESSION 02</div>
      <h2 class="ttl"><span class="grad">섹션 제목</span></h2>
      <p class="sub">한 줄 부제</p>
      <div class="bar"></div>
    </div>
  </section>

  <div class="counter"><span id="cur">1</span> / <span id="tot">1</span></div>

<script>
(function(){
  var slides=Array.prototype.slice.call(document.querySelectorAll('.slide'));
  var i=0,buf='';
  document.getElementById('tot').textContent=slides.length;
  function show(n){
    i=Math.max(0,Math.min(slides.length-1,n));
    slides.forEach(function(s,idx){s.classList.toggle('active',idx===i);});
    document.getElementById('cur').textContent=i+1;
  }
  document.addEventListener('keydown',function(e){
    var t=e.target.tagName;
    if(t==='INPUT'||t==='TEXTAREA')return;
    switch(e.key){
      case 'ArrowRight':case ' ':case 'PageDown':case 'j':case 'l':show(i+1);break;
      case 'ArrowLeft':case 'PageUp':case 'k':case 'h':show(i-1);break;
      case 'Home':show(0);break;
      case 'End':show(slides.length-1);break;
      case 'f':document.fullscreenElement?document.exitFullscreen():document.documentElement.requestFullscreen();break;
      case 'Escape':if(document.fullscreenElement)document.exitFullscreen();break;
      case 'Enter':if(buf){show(parseInt(buf,10)-1);buf='';}break;
      default:if(/^[0-9]$/.test(e.key)){buf+=e.key;return;}
    }
    buf='';
  });
  var sx=0,sy=0;
  document.addEventListener('touchstart',function(e){sx=e.changedTouches[0].clientX;sy=e.changedTouches[0].clientY;},{passive:true});
  document.addEventListener('touchend',function(e){
    var dx=e.changedTouches[0].clientX-sx,dy=e.changedTouches[0].clientY-sy;
    if(Math.abs(dx)>80&&Math.abs(dy)<40){dx<0?show(i+1):show(i-1);}
  },{passive:true});
  show(0);
})();
</script>
</body>
</html>
