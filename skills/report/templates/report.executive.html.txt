<!doctype html>
<!-- loop-kit / report : 보고서 결 [임원 요약결 Executive Brief]
     목적 : 의사결정자가 10초 안에 결론을 파악하고 행동을 결정한다.
     원칙 : 결론 먼저 → 수치 → 권고. 배경·근거는 요약만. 1~2 스크롤 이내.
     CSS는 <style>에 인라인. 외부 CDN 0개. @media print 필수. -->
<html lang="ko">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>보고서 제목</title>
<style>
:root{
  --bg:#f7f7f5; --surface:#ffffff; --border:#e2e2df;
  --text-hi:#0a0a0a; --text-md:#3a3a38; --text-lo:#8c8c86;
  --accent:#1a1a1a; --blue:#1d4ed8; --green:#15803d; --red:#b91c1c;
  --sans:-apple-system,'Segoe UI','Pretendard',sans-serif;
  --mono:'JetBrains Mono',ui-monospace,monospace;
  --max:860px;
}
*{box-sizing:border-box;margin:0;padding:0;}
body{font-family:var(--sans);background:var(--bg);color:var(--text-md);line-height:1.6;word-break:keep-all;}
.wrap{max-width:var(--max);margin:0 auto;padding:48px 32px 80px;}

/* 헤더 */
.doc-meta{font-family:var(--mono);font-size:13px;letter-spacing:.06em;color:var(--text-lo);margin-bottom:40px;display:flex;gap:24px;flex-wrap:wrap;border-bottom:1px solid var(--border);padding-bottom:16px;}
h1{font-size:36px;font-weight:700;color:var(--text-hi);line-height:1.2;letter-spacing:-.02em;margin-bottom:8px;}
.subtitle{font-size:18px;color:var(--text-lo);margin-bottom:32px;}

/* 한 줄 결론 : 가장 먼저 눈에 들어와야 함 */
.summary-box{background:var(--text-hi);color:#f7f7f5;padding:28px 32px;border-radius:6px;margin-bottom:32px;}
.summary-box .label{font-family:var(--mono);font-size:12px;letter-spacing:.14em;text-transform:uppercase;color:rgba(247,247,245,.5);margin-bottom:10px;}
.summary-box .conclusion{font-size:22px;font-weight:600;line-height:1.45;}

/* KPI : 수치 강조 */
.kpi-row{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-bottom:32px;}
.kpi{background:var(--surface);border:1px solid var(--border);border-top:3px solid var(--text-hi);padding:24px 22px;border-radius:4px;}
.kpi .v{font-size:44px;font-weight:700;color:var(--text-hi);letter-spacing:-.03em;line-height:1;}
.kpi.up .v{color:var(--green);} .kpi.down .v{color:var(--red);}
.kpi .l{font-size:14px;color:var(--text-lo);margin-top:8px;}
.kpi .delta{font-family:var(--mono);font-size:13px;color:var(--text-lo);margin-top:4px;}

/* 권고 행동 : 독자가 해야 할 일 */
.action-box{background:#eff6ff;border:1px solid #bfdbfe;border-left:4px solid var(--blue);padding:24px 28px;border-radius:4px;margin-bottom:32px;}
.action-box .label{font-family:var(--mono);font-size:12px;letter-spacing:.12em;text-transform:uppercase;color:var(--blue);margin-bottom:12px;}
.action-box ol{padding-left:20px;display:flex;flex-direction:column;gap:8px;}
.action-box li{font-size:16px;color:var(--text-hi);font-weight:500;}

/* 섹션 (배경·리스크는 접어두거나 짧게) */
.section{margin-bottom:32px;}
.section h2{font-size:18px;font-weight:600;color:var(--text-hi);margin-bottom:12px;padding-bottom:8px;border-bottom:1px solid var(--border);}
.section p{font-size:15px;line-height:1.7;}

/* 리스크 */
.risk-box{background:#fef2f2;border:1px solid #fecaca;border-left:4px solid var(--red);padding:16px 20px;border-radius:4px;font-size:15px;color:#7f1d1d;}

/* 데이터 표 */
.data-table{width:100%;border-collapse:collapse;font-size:15px;margin-top:12px;}
.data-table th{text-align:left;padding:10px 14px;background:var(--accent);color:#f7f7f5;font-size:13px;font-weight:600;}
.data-table td{padding:10px 14px;border-bottom:1px solid var(--border);}
.data-table tr:last-child td{border-bottom:none;}

/* 각주 */
.footnote{font-size:13px;color:var(--text-lo);border-top:1px solid var(--border);padding-top:16px;margin-top:40px;}

@media print{
  body{background:#fff;}
  .wrap{padding:24px 16px;}
  .summary-box{background:#000!important;-webkit-print-color-adjust:exact;print-color-adjust:exact;}
  .kpi-row{grid-template-columns:repeat(3,1fr);}
}
@media(max-width:640px){.kpi-row{grid-template-columns:1fr;}.wrap{padding:32px 20px;}}
</style>
</head>
<body>
<div class="wrap">

  <!-- 헤더 메타 -->
  <div class="doc-meta">
    <span>보고 일자 : 2026-00-00</span>
    <span>작성 : OOO</span>
    <span>수신 : OOO</span>
    <span>분류 : 내부 한정</span>
  </div>
  <h1>보고서 제목 : 결론이 제목이 된다</h1>
  <p class="subtitle">부제 또는 보고 범위 한 줄</p>

  <!-- ① 한 줄 결론 (가장 먼저) -->
  <div class="summary-box">
    <div class="label">핵심 결론</div>
    <div class="conclusion">이 보고서가 전하는 단 하나의 메시지를 한 문장으로. 결론이 먼저, 배경이 나중이다.</div>
  </div>

  <!-- ② 핵심 수치 (최대 3개) -->
  <div class="kpi-row">
    <div class="kpi up">
      <div class="v">+23%</div>
      <div class="l">수치 레이블</div>
      <div class="delta">전분기 대비</div>
    </div>
    <div class="kpi down">
      <div class="v">−12%</div>
      <div class="l">수치 레이블</div>
      <div class="delta">목표 대비</div>
    </div>
    <div class="kpi">
      <div class="v">3.4x</div>
      <div class="l">수치 레이블</div>
      <div class="delta">YoY</div>
    </div>
  </div>

  <!-- ③ 권고 행동 (독자가 해야 할 일) -->
  <div class="action-box">
    <div class="label">권고 행동</div>
    <ol>
      <li>권고 항목 1 : 구체적인 행동으로 서술한다</li>
      <li>권고 항목 2 : "검토가 필요합니다" 금지. "OO를 승인하세요"로</li>
      <li>권고 항목 3 : 기한이 있으면 함께 명시한다 (2026-00-00까지)</li>
    </ol>
  </div>

  <!-- ④ 배경·맥락 (짧게, 선택) -->
  <div class="section">
    <h2>배경</h2>
    <p>왜 이 보고가 필요한가를 2~3문장으로. 길게 쓰면 읽히지 않는다. 임원은 배경보다 결론을 먼저 본다.</p>
  </div>

  <!-- ⑤ 리스크 (선택) -->
  <div class="section">
    <h2>리스크 및 주의</h2>
    <div class="risk-box">가장 중요한 리스크 한 가지. 리스크는 숨기지 않는다. 조치 방안과 함께 제시한다.</div>
  </div>

  <!-- 각주 -->
  <div class="footnote">
    출처 : 데이터 출처를 명시한다. 작성일 기준. 수치는 내부 시스템 기준.
  </div>

</div>
</body>
</html>
