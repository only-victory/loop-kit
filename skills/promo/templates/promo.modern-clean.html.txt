<!doctype html>
<!-- loop-kit / promo : 디자인 결 [모던 클린]
     정체성 : 그로테스크·강한 그리드·블루 포인트. 기업 교육·테크 스쿨 무드. 유일한 라이트 결.
     색은 결에 포함. em-dash 금지(콜론 사용). 외부 JS 금지. -->
<html lang="ko">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>프로그램명</title>
<link href="https://fonts.googleapis.com/css2?family=Archivo:wght@500;700;900&family=JetBrains+Mono:wght@400;600&display=swap" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css" rel="stylesheet">
<style>
:root{
  --bg:#f5f5f3; --surface:#ffffff; --surface-2:#eeeeed;
  --fg:#0d0d0d; --mute:#3a3a38; --dim:#8c8c86;
  --blue:#1a5cfb; --blue-light:#eef2ff; --blue-dark:#0f3ccf;
  --line:#0d0d0d; --line-soft:#d4d4d0;
  --grot:'Archivo',-apple-system,sans-serif;
  --mono:'JetBrains Mono',ui-monospace,monospace;
  --sans:'Pretendard',-apple-system,sans-serif;
  --max:800px;
}
*{box-sizing:border-box;margin:0;padding:0;}
html{scroll-behavior:smooth;}
body{font-family:var(--sans);background:var(--bg);color:var(--mute);line-height:1.7;word-break:keep-all;}
.wrap{max-width:var(--max);margin:0 auto;padding:0 28px;}

/* ── 상단 바 ── */
.top-bar{background:var(--fg);color:#f5f5f3;padding:10px 0;}
.top-bar .inner{display:flex;justify-content:space-between;align-items:center;font-family:var(--mono);font-size:13px;}
.top-bar a{color:var(--blue);text-decoration:none;font-weight:600;}

/* ── 헤더·Hero ── */
header{padding:64px 0 72px;border-top:4px solid var(--fg);border-bottom:1px solid var(--line-soft);}
.eyebrow{font-family:var(--mono);font-size:13px;letter-spacing:.1em;text-transform:uppercase;color:var(--blue);margin-bottom:18px;display:flex;align-items:center;gap:10px;}
.eyebrow::after{content:'';flex:1;height:1px;background:var(--line-soft);max-width:120px;}
header h1{font-family:var(--grot);font-size:clamp(36px,6.5vw,72px);font-weight:900;color:var(--fg);line-height:.98;letter-spacing:-.03em;margin-bottom:20px;}
header h1 .blue{color:var(--blue);}
.hero-sub{font-size:18px;line-height:1.65;color:var(--mute);max-width:54ch;margin-bottom:32px;border-left:3px solid var(--blue);padding-left:16px;}
.btn-row{display:flex;gap:12px;flex-wrap:wrap;align-items:center;}
.btn-primary{display:inline-block;padding:14px 32px;background:var(--fg);color:#f5f5f3;font-family:var(--grot);font-size:15px;font-weight:700;letter-spacing:.02em;text-decoration:none;transition:background .15s;}
.btn-primary:hover{background:var(--blue);}
.btn-secondary{display:inline-block;padding:13px 24px;border:1px solid var(--line-soft);color:var(--mute);font-size:15px;text-decoration:none;}
.deadline{font-family:var(--mono);font-size:13px;color:var(--dim);margin-top:14px;}

/* ── meta chips ── */
.meta-chips{display:flex;flex-wrap:wrap;gap:0;padding:24px 0;border-bottom:1px solid var(--line-soft);}
.chip{font-family:var(--mono);font-size:13px;color:var(--mute);padding:8px 16px;border-right:1px solid var(--line-soft);}
.chip:first-child{padding-left:0;}
.chip .k{color:var(--blue);margin-right:5px;font-weight:700;}

/* ── 섹션 ── */
section{padding:64px 0;border-bottom:1px solid var(--line-soft);}
.section-label{font-family:var(--mono);font-size:12px;letter-spacing:.16em;text-transform:uppercase;color:var(--dim);margin-bottom:8px;}
h2{font-family:var(--grot);font-size:32px;font-weight:900;color:var(--fg);line-height:1.1;letter-spacing:-.02em;margin-bottom:20px;}
p{font-size:16px;line-height:1.75;margin-bottom:12px;}

/* ── 교육 개요 표 ── */
.info-table{width:100%;border-collapse:collapse;margin-top:8px;}
.info-table th{width:130px;text-align:left;padding:12px 0;font-family:var(--mono);font-size:13px;color:var(--dim);border-bottom:1px solid var(--line-soft);vertical-align:top;}
.info-table td{padding:12px 0 12px 12px;font-size:16px;color:var(--fg);border-bottom:1px solid var(--line-soft);}
.info-table tr:last-child th,.info-table tr:last-child td{border-bottom:none;}

/* ── 추천 대상 ── */
.recommend-grid{display:grid;grid-template-columns:1fr 1fr;gap:1px;background:var(--line-soft);border:1px solid var(--line-soft);margin-top:16px;}
.rec-card{padding:24px;background:var(--surface);}
.rec-card .num{font-family:var(--mono);font-size:12px;color:var(--blue);font-weight:700;margin-bottom:10px;}
.rec-card .title{font-family:var(--grot);font-size:17px;font-weight:700;color:var(--fg);margin-bottom:8px;}
.rec-card p{font-size:14px;color:var(--mute);line-height:1.65;margin-bottom:0;}

/* ── 커리큘럼 ── */
.curri-stack{display:flex;flex-direction:column;gap:0;border:1px solid var(--line-soft);margin-top:16px;}
.curri-item{display:flex;gap:0;border-bottom:1px solid var(--line-soft);}
.curri-item:last-child{border-bottom:none;}
.curri-num{font-family:var(--mono);font-size:13px;font-weight:700;color:var(--blue);min-width:56px;padding:18px 16px;border-right:1px solid var(--line-soft);display:flex;align-items:flex-start;}
.curri-content{padding:18px 20px;flex:1;}
.curri-content .title{font-size:16px;font-weight:600;color:var(--fg);margin-bottom:4px;}
.curri-content .desc{font-size:14px;color:var(--mute);}
.curri-content .badge{display:inline-block;font-family:var(--mono);font-size:11px;color:var(--blue);border:1px solid var(--blue-light);background:var(--blue-light);padding:2px 8px;margin-top:6px;letter-spacing:.04em;}

/* ── 강사 ── */
.instructor-card{display:grid;grid-template-columns:80px 1fr;gap:24px;padding:24px;background:var(--surface);border:1px solid var(--line-soft);margin-top:16px;}
.instructor-img{width:80px;height:80px;background:var(--surface-2);display:flex;align-items:center;justify-content:center;font-size:28px;flex-shrink:0;}
.instructor-info .name{font-family:var(--grot);font-size:20px;font-weight:900;color:var(--fg);margin-bottom:4px;}
.instructor-info .title{font-family:var(--mono);font-size:13px;color:var(--blue);margin-bottom:10px;}
.instructor-info p{font-size:14px;color:var(--mute);line-height:1.7;margin-bottom:0;}

/* ── 신청 CTA ── */
.apply-box{padding:64px 0;}
.apply-box .inner{background:var(--fg);padding:48px 40px;}
.apply-box h2{font-family:var(--grot);font-size:clamp(28px,4.5vw,48px);font-weight:900;color:#f5f5f3;letter-spacing:-.03em;line-height:1.1;margin-bottom:12px;}
.apply-box h2 .blue{color:var(--blue);}
.apply-box .sub{font-size:16px;color:rgba(245,245,243,.6);margin-bottom:28px;}
.apply-box .btn-cta{display:inline-block;padding:16px 40px;background:var(--blue);color:#f5f5f3;font-family:var(--grot);font-size:16px;font-weight:700;text-decoration:none;letter-spacing:.02em;}
.apply-box .btn-cta:hover{background:var(--blue-dark);}
.apply-box .deadline{font-family:var(--mono);font-size:13px;color:rgba(245,245,243,.45);margin-top:14px;}

/* ── footer ── */
footer{padding:28px 0;border-top:1px solid var(--line-soft);}
footer p{font-family:var(--mono);font-size:13px;color:var(--dim);line-height:1.8;}

@media(max-width:640px){
  header{padding:44px 0 52px;}
  .recommend-grid{grid-template-columns:1fr;}
  section{padding:44px 0;}
  .btn-row{flex-direction:column;align-items:flex-start;}
  .top-bar .deadline-notice{display:none;}
}
</style>
</head>
<body>

<div class="top-bar">
  <div class="wrap">
    <div class="inner">
      <span>신청 마감 : 2026-00-00 &nbsp;·&nbsp; 선착순 00명</span>
      <a href="신청링크">지금 신청하기 →</a>
    </div>
  </div>
</div>

<div class="wrap">

  <header>
    <div class="eyebrow">기업 교육 · 2026</div>
    <h1>프로그램 핵심 가치를<br><span class="blue">한 문장</span>으로 담는다</h1>
    <p class="hero-sub">Hero 부제는 "왜 이 프로그램인가"를 한 문장으로. 프로그램명을 반복하지 않는다.</p>
    <div class="btn-row">
      <a class="btn-primary" href="신청링크">신청하기</a>
      <a class="btn-secondary" href="#curriculum">커리큘럼 보기</a>
    </div>
    <p class="deadline">신청 마감 : 2026-00-00</p>
  </header>

  <div class="meta-chips">
    <span class="chip"><span class="k">기간</span>0주 / 0회차</span>
    <span class="chip"><span class="k">장소</span>온라인</span>
    <span class="chip"><span class="k">대상</span>대상 한 줄</span>
    <span class="chip"><span class="k">인원</span>00명</span>
    <span class="chip"><span class="k">형식</span>강의 + 실습</span>
  </div>

  <section>
    <div class="section-label">Program Overview</div>
    <h2>교육 개요</h2>
    <table class="info-table">
      <tr><th>일정</th><td>2026-00-00 ~ 2026-00-00</td></tr>
      <tr><th>시간</th><td>매주 OO요일 OO시 ~ OO시</td></tr>
      <tr><th>장소</th><td>장소 또는 온라인 플랫폼</td></tr>
      <tr><th>모집 인원</th><td>00명</td></tr>
      <tr><th>수강료</th><td>OO만 원</td></tr>
      <tr><th>문의</th><td>oo@example.com</td></tr>
    </table>
  </section>

  <section>
    <div class="section-label">Who Is This For</div>
    <h2>이런 분께 추천합니다</h2>
    <div class="recommend-grid">
      <div class="rec-card"><div class="num">01</div><div class="title">페르소나 1</div><p>이 페르소나가 가진 문제나 목표를 구체적으로.</p></div>
      <div class="rec-card"><div class="num">02</div><div class="title">페르소나 2</div><p>이 페르소나가 가진 문제나 목표를 구체적으로.</p></div>
      <div class="rec-card"><div class="num">03</div><div class="title">페르소나 3</div><p>이 페르소나가 가진 문제나 목표를 구체적으로.</p></div>
      <div class="rec-card"><div class="num">×</div><div class="title">이런 분은 아닙니다</div><p>기대치를 조율한다. 맞지 않는 대상을 명확히 해야 이탈이 줄어든다.</p></div>
    </div>
  </section>

  <section id="curriculum">
    <div class="section-label">Curriculum</div>
    <h2>커리큘럼</h2>
    <div class="curri-stack">
      <div class="curri-item">
        <div class="curri-num">01</div>
        <div class="curri-content">
          <div class="title">1회차 제목</div>
          <div class="desc">이 회차에서 다루는 내용을 한 줄로.</div>
          <span class="badge">강의</span>
        </div>
      </div>
      <div class="curri-item">
        <div class="curri-num">02</div>
        <div class="curri-content">
          <div class="title">2회차 제목</div>
          <div class="desc">이 회차에서 다루는 내용을 한 줄로.</div>
          <span class="badge">실습</span>
        </div>
      </div>
      <div class="curri-item">
        <div class="curri-num">03</div>
        <div class="curri-content">
          <div class="title">3회차 제목</div>
          <div class="desc">이 회차에서 다루는 내용을 한 줄로.</div>
          <span class="badge">강의 + 실습</span>
        </div>
      </div>
    </div>
  </section>

  <section>
    <div class="section-label">Instructor</div>
    <h2>강사 소개</h2>
    <div class="instructor-card">
      <div class="instructor-img">👤</div>
      <div class="instructor-info">
        <div class="name">강사명</div>
        <div class="title">직책 · 소속</div>
        <p>강사 소개 2~3문장. 강사가 왜 이 과정을 가르칠 자격이 있는지.</p>
      </div>
    </div>
  </section>

</div>

<div class="apply-box">
  <div class="wrap">
    <div class="inner">
      <h2>지금 시작하면<br><span class="blue">0주 후</span> 달라진다</h2>
      <p class="sub">신청 마감 : 2026-00-00 &nbsp;·&nbsp; 선착순 00명</p>
      <a class="btn-cta" href="신청링크">마감 전 신청하기</a>
      <p class="deadline">조기 등록 할인 : ~2026-00-00</p>
    </div>
  </div>
</div>

<div class="wrap">
  <footer>
    <p>주관 기관명 &nbsp;·&nbsp; 문의 : oo@example.com<br>© 2026 기관명. All rights reserved.</p>
  </footer>
</div>

</body>
</html>
