<!doctype html>
<!-- loop-kit / promo : 디자인 결 [프리미엄 다크]
     정체성 : 세리프 헤드라인·넓은 여백·금/크림 강조. 고급 브랜드·마스터클래스 무드.
     색은 결에 포함. em-dash 금지(콜론 사용). 외부 JS 금지. -->
<html lang="ko">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>프로그램명</title>
<link href="https://fonts.googleapis.com/css2?family=Noto+Serif+KR:wght@400;600;700&family=JetBrains+Mono:wght@400;600&display=swap" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css" rel="stylesheet">
<style>
:root{
  --bg:#09090f; --surface:#13131c; --surface-hi:#1c1c2a;
  --fg:#ede9df; --mute:#a09880; --dim:#5a5748;
  --gold:#c8963b; --cream:#ede9df; --olive:#7d7c54;
  --border:#21201a; --border-hi:#3a3828;
  --serif:'Noto Serif KR',Georgia,serif;
  --mono:'JetBrains Mono',ui-monospace,monospace;
  --sans:'Pretendard',-apple-system,sans-serif;
  --max:760px;
}
*{box-sizing:border-box;margin:0;padding:0;}
html{scroll-behavior:smooth;}
body{font-family:var(--sans);background:var(--bg);color:var(--mute);line-height:1.75;word-break:keep-all;}
.wrap{max-width:var(--max);margin:0 auto;padding:0 28px;}

/* ── 헤더·Hero ── */
header{padding:80px 0 96px;border-bottom:1px solid var(--border);}
.eyebrow{font-family:var(--mono);font-size:13px;letter-spacing:.18em;text-transform:uppercase;color:var(--gold);margin-bottom:24px;}
header h1{font-family:var(--serif);font-size:clamp(38px,6vw,68px);font-weight:700;color:var(--fg);line-height:1.18;letter-spacing:-.01em;margin-bottom:20px;}
header h1 em{font-style:italic;color:var(--gold);}
.hero-sub{font-size:18px;line-height:1.7;color:var(--mute);max-width:52ch;margin-bottom:36px;}
.btn-primary{display:inline-block;padding:16px 36px;border:1px solid var(--gold);color:var(--gold);font-family:var(--sans);font-size:16px;font-weight:600;letter-spacing:.04em;text-decoration:none;transition:all .2s;}
.btn-primary:hover{background:var(--gold);color:var(--bg);}
.deadline{font-family:var(--mono);font-size:13px;color:var(--dim);margin-top:14px;}

/* ── meta chips ── */
.meta-chips{display:flex;flex-wrap:wrap;gap:10px;padding:32px 0;border-bottom:1px solid var(--border);}
.chip{font-family:var(--mono);font-size:13px;color:var(--mute);padding:7px 16px;border:1px solid var(--border-hi);}
.chip .k{color:var(--gold);margin-right:6px;}

/* ── 섹션 공통 ── */
section{padding:72px 0;border-bottom:1px solid var(--border);}
.section-label{font-family:var(--mono);font-size:12px;letter-spacing:.2em;text-transform:uppercase;color:var(--gold);margin-bottom:20px;}
h2{font-family:var(--serif);font-size:36px;font-weight:600;color:var(--fg);line-height:1.25;margin-bottom:16px;}
p{font-size:16px;line-height:1.8;margin-bottom:14px;}

/* ── 교육 개요 표 ── */
.info-table{width:100%;border-collapse:collapse;margin-top:8px;}
.info-table th{width:140px;text-align:left;padding:14px 0;font-family:var(--mono);font-size:13px;color:var(--gold);border-bottom:1px solid var(--border);vertical-align:top;}
.info-table td{padding:14px 0 14px 16px;font-size:16px;color:var(--mute);border-bottom:1px solid var(--border);}

/* ── 추천 대상 ── */
.recommend-grid{display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-top:16px;}
.rec-card{padding:24px;border:1px solid var(--border-hi);background:var(--surface);}
.rec-card .icon{font-size:22px;margin-bottom:12px;}
.rec-card .title{font-family:var(--serif);font-size:17px;color:var(--fg);margin-bottom:6px;}
.rec-card p{font-size:14px;color:var(--mute);line-height:1.65;margin-bottom:0;}

/* ── 커리큘럼 ── */
.curri-stack{display:flex;flex-direction:column;gap:0;margin-top:16px;}
.curri-item{display:flex;gap:20px;padding:20px 0;border-bottom:1px solid var(--border);}
.curri-item:last-child{border-bottom:none;}
.curri-num{font-family:var(--serif);font-style:italic;font-size:28px;color:var(--gold);min-width:40px;}
.curri-content .title{font-size:17px;font-weight:600;color:var(--fg);margin-bottom:4px;}
.curri-content .desc{font-size:14px;color:var(--mute);}
.curri-content .badge{display:inline-block;font-family:var(--mono);font-size:11px;color:var(--olive);border:1px solid var(--olive);padding:2px 8px;margin-top:6px;letter-spacing:.06em;}

/* ── 강사 ── */
.instructor-card{display:flex;gap:28px;padding:28px;background:var(--surface);border:1px solid var(--border-hi);margin-top:16px;}
.instructor-img{width:80px;height:80px;border-radius:50%;background:var(--surface-hi);flex-shrink:0;display:flex;align-items:center;justify-content:center;font-size:28px;}
.instructor-info .name{font-family:var(--serif);font-size:20px;color:var(--fg);margin-bottom:4px;}
.instructor-info .title{font-family:var(--mono);font-size:13px;color:var(--gold);margin-bottom:10px;}
.instructor-info p{font-size:14px;color:var(--mute);line-height:1.7;margin-bottom:0;}

/* ── 신청 CTA ── */
.apply-box{padding:72px 0;}
.apply-box .inner{border:1px solid var(--border-hi);padding:48px 40px;text-align:center;}
.apply-box blockquote{font-family:var(--serif);font-style:italic;font-size:clamp(24px,4vw,40px);color:var(--fg);line-height:1.35;margin-bottom:32px;}
.apply-box blockquote em{color:var(--gold);font-style:normal;}
.apply-box .deadline{font-family:var(--mono);font-size:14px;color:var(--dim);margin-bottom:20px;}
.apply-box .btn-primary{font-size:17px;padding:18px 48px;}

/* ── footer ── */
footer{padding:32px 0;text-align:center;}
footer p{font-family:var(--mono);font-size:13px;color:var(--dim);line-height:1.9;}

@media(max-width:640px){
  header{padding:56px 0 64px;}
  .recommend-grid{grid-template-columns:1fr;}
  .instructor-card{flex-direction:column;}
  section{padding:48px 0;}
}
</style>
</head>
<body>
<div class="wrap">

  <!-- Hero -->
  <header>
    <div class="eyebrow">2026 · 과정 카테고리</div>
    <h1>프로그램 핵심 가치를<br><em>한 문장</em>으로 담는다</h1>
    <p class="hero-sub">Hero 부제는 "왜 이 프로그램인가"를 한 문장으로. 프로그램명을 반복하지 않는다.</p>
    <a class="btn-primary" href="신청링크">지금 신청하기</a>
    <p class="deadline">신청 마감 : 2026-00-00</p>
  </header>

  <!-- meta chips -->
  <div class="wrap">
    <div class="meta-chips">
      <span class="chip"><span class="k">기간</span>0주 / 0회차</span>
      <span class="chip"><span class="k">장소</span>온라인 · 오프라인</span>
      <span class="chip"><span class="k">대상</span>대상 한 줄</span>
      <span class="chip"><span class="k">인원</span>00명 이하</span>
      <span class="chip"><span class="k">형식</span>강의 + 실습</span>
    </div>
  </div>

  <!-- 교육 개요 -->
  <section>
    <div class="section-label">Program Overview</div>
    <h2>교육 개요</h2>
    <table class="info-table">
      <tr><th>일정</th><td>2026-00-00 ~ 2026-00-00</td></tr>
      <tr><th>시간</th><td>매주 OO요일 OO시 ~ OO시</td></tr>
      <tr><th>장소</th><td>장소 상세 또는 온라인 링크</td></tr>
      <tr><th>모집 인원</th><td>00명 (선착순 / 서류 심사)</td></tr>
      <tr><th>수강료</th><td>OO만 원 (조기 등록 할인 : ~2026-00-00)</td></tr>
      <tr><th>문의</th><td>oo@example.com</td></tr>
    </table>
  </section>

  <!-- 추천 대상 (선택) -->
  <section>
    <div class="section-label">Who Is This For</div>
    <h2>이런 분께 추천합니다</h2>
    <div class="recommend-grid">
      <div class="rec-card"><div class="icon">✦</div><div class="title">페르소나 1</div><p>이 페르소나가 가진 문제나 목표를 구체적으로.</p></div>
      <div class="rec-card"><div class="icon">◆</div><div class="title">페르소나 2</div><p>이 페르소나가 가진 문제나 목표를 구체적으로.</p></div>
      <div class="rec-card"><div class="icon">▸</div><div class="title">페르소나 3</div><p>이 페르소나가 가진 문제나 목표를 구체적으로.</p></div>
      <div class="rec-card"><div class="icon">◇</div><div class="title">이런 분은 아닙니다</div><p>기대치를 조율한다. 맞지 않는 대상을 명확히 해야 이탈이 줄어든다.</p></div>
    </div>
  </section>

  <!-- 커리큘럼 -->
  <section>
    <div class="section-label">Curriculum</div>
    <h2>커리큘럼</h2>
    <div class="curri-stack">
      <div class="curri-item">
        <div class="curri-num">i</div>
        <div class="curri-content">
          <div class="title">1회차 제목</div>
          <div class="desc">이 회차에서 다루는 내용을 한 줄로.</div>
          <span class="badge">강의</span>
        </div>
      </div>
      <div class="curri-item">
        <div class="curri-num">ii</div>
        <div class="curri-content">
          <div class="title">2회차 제목</div>
          <div class="desc">이 회차에서 다루는 내용을 한 줄로.</div>
          <span class="badge">실습</span>
        </div>
      </div>
      <div class="curri-item">
        <div class="curri-num">iii</div>
        <div class="curri-content">
          <div class="title">3회차 제목</div>
          <div class="desc">이 회차에서 다루는 내용을 한 줄로.</div>
          <span class="badge">강의 + 실습</span>
        </div>
      </div>
    </div>
  </section>

  <!-- 강사 소개 (강사 기반 필수) -->
  <section>
    <div class="section-label">Instructor</div>
    <h2>강사 소개</h2>
    <div class="instructor-card">
      <div class="instructor-img">👤</div>
      <div class="instructor-info">
        <div class="name">강사명</div>
        <div class="title">직책 · 소속</div>
        <p>강사 소개는 신뢰의 핵심이다. 강사가 왜 이 과정을 가르칠 자격이 있는지, 수강생에게 어떤 가치를 줄 수 있는지를 2~3문장으로.</p>
      </div>
    </div>
  </section>

  <!-- 신청 CTA -->
  <div class="apply-box">
    <div class="inner">
      <blockquote>배우는 것보다<br><em>시작하는 것</em>이 어렵다.</blockquote>
      <p class="deadline">신청 마감 : 2026-00-00 | 선착순 00명</p>
      <a class="btn-primary" href="신청링크">마감 전 신청하기</a>
    </div>
  </div>

  <footer>
    <p>주관 기관명 · 문의 : oo@example.com<br>© 2026 기관명. All rights reserved.</p>
  </footer>

</div>
</body>
</html>
